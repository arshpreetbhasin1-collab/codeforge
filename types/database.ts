/**
 * Placeholder for Supabase's generated database types.
 *
 * Once a real Supabase project exists, regenerate this file with:
 *   npx supabase gen types typescript --project-id <id> > types/database.ts
 *
 * Until then, `Database` is left loose so lib/db/supabase-*.ts type-checks
 * without a live project. Domain-level types that the app actually codes
 * against live in types/domain.ts instead — prefer those over reaching into
 * `Database["public"]["Tables"]` directly.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
